步骤：
1、在表adm_printset的ext3字段设置纸张长宽
2、设置对应lodop目录下的打印模板文件
具体可以参照椒江（套打）和余杭（白纸打）

特殊说明：
1、整个页面就是一个html片段
2、所有元素都是由绝对定位来操作(position:absolute;left:xxx;right:xxx)，因此可以动态调整
3、所有元素可以通过设置字体为白色来控制不显示且不打印
4、data-rows=4 表示设置商品明细表格的行数 
5、data-line=0 标识商品明细表格是否需要边框线条